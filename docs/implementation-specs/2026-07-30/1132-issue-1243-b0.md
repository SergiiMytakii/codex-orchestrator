# Issue #1243 B0 baseline

Recorded before any production edit in the dedicated implementation worktree.

```text
git rev-parse HEAD
7b2a002773dfc3faa87a28c4c5e60641bcb332bf

git ls-files 'src/v2/*.ts' 'src/v2/**/*.ts' | LC_ALL=C sort | xargs wc -l
22704 total

wc -l src/v2/run-issue.ts
4142 src/v2/run-issue.ts
```

## Before owner map

Run-lifecycle and progression owners currently present in production state and
runtime:

- `RunStateFileV1` / `RunRecordV1`, using
  `codex-orchestrator.agent-auto-state` with V1/V2/V3 compatibility,
  migration backup/metadata, and rollback.
- `RunRecord.process`, the generic process owner.
- `routeExecution` and its route/report invocations.
- `waitingHuman` and its separate comment/label intent progression.
- `specDelivery` with spec-author and spec-review invocation ownership.
- `directReview` with Full/Closure stages and review invocation ownership.
- `reviewFeedback` with a separate phase machine and
  `implementationInvocation`.
- `checkQualification.repairInvocation`, the qualification-repair lifecycle.
- `executionLease`, the candidate operation/process owner.
- proof identity/receipt plus the separate `proof-store`.
- `PublicationIntent`, containing initial and post-PR Git/GitHub effect
  progression.

Retained finite safety resources that are not Run-lifecycle progression owners:
repository owner-control lock, atomic state-file lock, candidate refs and
materializations, and mobile/device leases.

## Superseded mechanisms targeted by #1243

- Legacy state readers/conversion and V3 migration backup/rollback.
- Duplicate route, spec, review, and report invocation records.
- Waiting-human, review-feedback, and qualification operation-specific process
  owners.
- Candidate execution lease as a second process owner.
- Separate proof store/state.
- Parallel publication/effect owners and their superseded validators.

This baseline is the immutable comparison authority for #1246.
