interface:
  display_name: "To Tickets"
  short_description: "Review and publish executable ticket packets"
  default_prompt: "Use $to-tickets to compile and fully review each settled packet revision, obtain one approval, publish serially, verify tracker read-back, and stop before implementation."
policy:
  allow_implicit_invocation: false
