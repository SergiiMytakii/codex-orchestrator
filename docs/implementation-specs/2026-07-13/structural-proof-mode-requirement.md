# Structural proof-mode requirement

## Contract Test Ledger

| Invariant | Risk It Prevents | First Test / Proof | Status |
| --- | --- | --- | --- |
| `auto` plus frontend paths accepts mapped non-visual evidence through final publication gates, including strict legacy visual policy. | File location falsely forces browser Visual Proof for a non-visual refactor before or after Acceptance Proof. | `auto proof strategy accepts CLI evidence for a non-visual frontend refactor`; `implementation publishability accepts non-visual proof plan from the completion report without runner command proof` | green |
| Explicit `none`, non-visual, browser, mobile, and generic visual requirements enforce compatible plan modes. | An agent skips required proof or silently changes the required proof class. | `explicit proof requirements enforce the complete mode matrix`; `proof plan validation rejects visual strategy downgrades` | green |
| A validated browser/mobile plan controls runner command dispatch. | Late changed-path inference executes a different proof target than the accepted plan. | `visual-proof auto uses the validated proof plan mode instead of changed-path inference` | green |
| `auto` prompt guidance is neutral for non-visual work and conditional for genuine visual work. | The prompt predeclares browser proof or omits instructions needed after a visual choice. | `auto proof strategy does not predeclare browser proof for a non-visual frontend refactor`; `auto proof strategy keeps runner visual guidance conditional on the selected proof plan` | green |
| Self-improvement discovery emits one explicit non-`auto` strategy chosen from observable behavior. | Generated issues fall back to ambiguous path-based proof selection. | `discovery creates exactly one agent:auto self-improvement issue for the first valid candidate`; `invalid discovery report creates no issue` | green |
