interface:
  display_name: "Test-Driven Development"
  short_description: "Use TDD only when its behavioral fit gate passes"
  default_prompt: "Use $tdd to classify targeted proof versus Contract Test Ledger, then run RED to GREEN when the observable-behavior Fit Gate passes."
policy:
  allow_implicit_invocation: true
