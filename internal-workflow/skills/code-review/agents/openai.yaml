interface:
  display_name: "Review"
  short_description: "Parallel Spec and Standards review"
  default_prompt: "Use $code-review to review the settled change with distinct fresh Spec and Standards reviewers in parallel."
policy:
  allow_implicit_invocation: true
