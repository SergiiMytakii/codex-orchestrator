## Contract Test Ledger

| Invariant | Risk It Prevents | First Test / Proof | Status |
| --- | --- | --- | --- |
| The proof generation schema stays below 25 KB while every accepted report still passes the unchanged runtime validator. | Universal browser, Android, and iOS branch duplication turns trivial non-visual proof into a 52 KB structured-output request and increases model stream failures. | `proof generation schema stays compact while runtime owns platform combinations` in `test/v2-report-contracts.test.ts` | green |
| Runtime validation continues rejecting invalid status, evidence, target, and publication combinations omitted from the compact generation schema. | Schema compaction could weaken the final proof contract or allow unsafe evidence publication. | Existing runtime validator tests plus compact-schema regression fixtures in `test/v2-report-contracts.test.ts` | green |
| A successful Codex exit that fails to publish its output report is retried as transport failure; malformed or unsafe report bytes remain report failures. | Transient output delivery loss terminally blocks triage/review even though the operation is safe to rerun. | `classifies successful Codex exit without its output report as resumable transport failure` in `test/v2-codex-process.test.ts` | green |
| One schema-valid implementation report with an incomplete cumulative `changedFiles` list receives a report-only repair against the canonical Git change set, without mutating the worktree or consuming an implementation cycle. | A model can correctly finish a later repair while reporting only that cycle's files, causing production to reject an otherwise valid cumulative implementation. | `incomplete cumulative changedFiles gets one report-only repair without consuming a cycle` in `test/v2-run-issue.test.ts` | green |
| A repeated cumulative `changedFiles` mismatch remains terminal and fail-closed. | Recovery could otherwise loop indefinitely or accept a report that does not describe the actual product change set. | `repeated cumulative changedFiles mismatch remains fail-closed` in `test/v2-run-issue.test.ts` | green |
