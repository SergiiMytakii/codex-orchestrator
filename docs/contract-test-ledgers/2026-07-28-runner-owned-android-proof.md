## Contract Test Ledger

| Invariant | Risk It Prevents | First Test / Proof | Status |
| --- | --- | --- | --- |
| A configured Android proof is prepared entirely by the trusted Runner on a newly started emulator with ephemeral clean data, even when a physical device or user emulator is present. | The contained proof worker cannot inspect host processes or use adb, takes over a user-owned runtime, or captures user data from a shared AVD. | `v2-android-proof-runner.test.ts`: `runner starts an isolated emulator and captures bound proof evidence without touching observed devices` | green |
| The active Android lease is written by the Runner to durable lease state and the proof artifact before the contained worker starts; terminal release matches PID plus process-start identity, is replay-safe, stops only its emulator, and removes only its validated data directory. | The helper tries to write `~/.codex-orchestrator` from a worktree-only sandbox, cleanup damages a foreign runtime/path after serial reuse, or a repeated release leaves an unrecoverable lease. | `v2-android-proof-runner.test.ts`: `runner owns lease creation and terminal release stops only its emulator`; `runner release is idempotent and never kills a foreign emulator after serial reuse` | green |
| Runner-created Android artifacts satisfy freshness only when explicitly bound to the current proof ID and checked change. | Pre-existing or foreign screenshots can be accepted as current visual proof, or trusted runner evidence is rejected because the worker did not rewrite it. | `v2-android-proof.test.ts`: `AcceptanceProof accepts current Runner-prepared Android artifacts without requiring the contained worker to rewrite them` | green |
| Flutter validation used by Acceptance Proof comes only from configured Runner checks and a fresh `build apk` output bound by APK and checked-change digests; blocked builds/navigation retain redacted diagnostics. | A contained proof worker cannot bind Flutter's loopback test socket, a stale APK is accepted after an unrelated zero-exit command, an untrusted worker escalates arbitrary commands into Runner authority, or a real blocker is collapsed into an opaque internal error. | `v2-android-proof-runner.test.ts`: Runner receipt assertions, `runner refuses a stale APK when the configured build produces no fresh output`, and `runner returns a useful redacted build blocker and cleans its lease and emulator` | green |
