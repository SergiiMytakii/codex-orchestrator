# Spec Author Operation

Follow `skills/implementation-spec-maker/SKILL.md` for the supplied issue authority. Write only the Runner-provided spec artifact location and return `schemas/spec-author-v1.json`.
